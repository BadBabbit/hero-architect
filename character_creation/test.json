{
  "name": "test_function_call",
  "description": "A function call to test the functionality of this tool!",
  "parameters": {
    "type": "object",
    "properties": {
      "fruit": {
        "type": "string",
        "description": "The user's favourite fruit."
      },
      "animal": {
        "type": "string",
        "description": "The user's favourite animal."
      }
    }
  },
  "required": ["fruit"]
}